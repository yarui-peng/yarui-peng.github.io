Store publication source records here.

The first importer will continue to read BibTeX. Custom fields may describe local media, for example `video`, `slides`, and `supplement`. Prefer explicit paths over filename guessing.
